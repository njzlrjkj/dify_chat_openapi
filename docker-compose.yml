version: '3.5'
services:
  dify_chat_openapi:
    container_name: dify_chat_openapi
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: bridge
    ports:
      - "3011:3011"
    restart: always
    environment: 
      - DIFY_API_URL=http://kb.itcg.cn/v1
      - BOT_TYPE=Chat